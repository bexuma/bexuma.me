import styled from 'styled-components';

const Project = styled.div`
  margin-bottom: 24px;
`;

const TagContainer = styled.div``;

const Tag = styled.div`
  display: inline-block;
  margin-right: 8px;
`;

export { Project, TagContainer, Tag };
